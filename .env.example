# Configure these values only in Render/local environment variables.
# Never commit a real connection string or credential.
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/sbdcs_db

# Authentication
JWT_SECRET=replace-with-a-long-random-secret
